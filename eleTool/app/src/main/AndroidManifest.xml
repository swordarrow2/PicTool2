<?xml version='1.0' encoding='utf-8'?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.meng.eleTool">

    <!-- ææç§çåè§é¢ -->
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- FLASHLIGHT -->
    <uses-permission android:name="android.permission.FLASHLIGHT"/>

    <!-- æ¥çç½ç»è¿æ¥ -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- æ§å¶æ¯å¨ -->
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!-- è¯»åæ¨å±äº«å­å¨ç©ºé´ä¸­çåå®¹ -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <!-- ä¿®æ¹æå é¤æ¨å±äº«å­å¨ç©ºé´ä¸­çåå®¹ -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <!-- æ­¤åºç¨å¯æ¾ç¤ºå¨å¶ä»åºç¨ä¸æ¹ -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>

    <!-- æ¥æå®å¨çç½ç»è®¿é®æé -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- è®¾ç½®å£çº¸ -->
    <uses-permission android:name="android.permission.SET_WALLPAPER"/>

    <!-- è°æ´æ¨çå£çº¸å¤§å° -->
    <uses-permission android:name="android.permission.SET_WALLPAPER_HINTS"/>

    <!-- BIND_WALLPAPER -->
    <uses-permission android:name="android.permission.BIND_WALLPAPER"/>

    <!-- READ_WALLPAPER_INTERNAL -->
    <uses-permission android:name="android.permission.READ_WALLPAPER_INTERNAL"/>

    <!-- SET_WALLPAPER_COMPONENT -->
    <uses-permission android:name="android.permission.SET_WALLPAPER_COMPONENT"/>

    <!-- MANAGE_USB -->
    <uses-permission android:name="android.permission.MANAGE_USB"/>

    <!-- WRITE_MEDIA_STORAGE -->
    <uses-permission android:name="android.permission.WRITE_MEDIA_STORAGE"/>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/green">

        <meta-data
            android:name="android.max_aspect"
            android:value="2.1"/>

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/green"
            android:windowSoftInputMode="adjustUnspecified|stateHidden">

            <intent-filter>

                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>

            </intent-filter>

        </activity>

        <activity
            android:name="com.meng.eleTool.activity.ModelEditActivity"
            android:label="app_name" />
        <activity
            android:name="com.meng.eleTool.activity.RecordEditActivity"
            android:label="app_name" />
        
		<activity android:name="com.meng.eleTool.SearchSemiee" />

    </application>

</manifest>
